{
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["complete", "incomplete", "invalid", "unknown"]
    }
  },
  "required": ["status"],
  "if": {
    "properties": {
      "status": {
        "const": "incomplete"
      }
    }
  },
  "then": {
    "properties": {
      "indent": {
        "type": "string"
      }
    },
    "required": ["indent"],
    "additionalProperties": false
  },
  "else": {
    "additionalProperties": false
  }
}
